This is the UART example from the Bleak repo:

https://github.com/hbldh/bleak

You can find the example file at examples/uart_service.py


To run, first install the library (in virtual environment?):

$ pip install bleak

next run the code:

$ python uart_client.py

