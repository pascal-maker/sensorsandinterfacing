lets start with explaining screen 3: we import lcd library which is used to control the lcd screen and time library for delays we use the same setup as screen 2 but instead of reading buttons we read the analog values from the potentiometers we use the ADC_ADRESS 0x48 to read the analog values from the potentiometers. we use a joystick with 2 potentiometers one for x axis and one for y axis we use the ADC_CHANNEL 5 to read the analog value from the x axis potentiometer and the ADC_CHANNEL 4 to read the analog value from the y axis potentiometer we do toggle enable and send byte the same way as screen 2 we do the same for lcd init and lcd string and lcd  we setup the lcd and toggle_enable bits send bits , we start with the command def ads7830_command(channel):#send a command to the adc rearranges the bits of the channel number into the format required by the ADS7830 creates the correct command byte for the ADC so the raspberry pi can tell the ADC which analog channel must be read. def read_adc(channel):#read the value from the adc sends the command to the adc and reads the value from the adc def make_bar(value):#make a bar graph calculates the number of blocks to display and returns the bar def wait_or_stop(stop_event,seconds):#wait for the given seconds or until the stop event is set def run(stop_event):#run the screen initializes the lcd reads the analog values from the potentiometers formats the values to be displayed on the lcd and displays them on the lcd
